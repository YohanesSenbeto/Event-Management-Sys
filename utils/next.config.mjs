// This is the configuration file for a Next.js application.
// It exports the NextConfig object, which allows customization of the Next.js behavior.
// For example, you can add custom webpack configurations, environment variables, or experimental features here.

/** @type {import('next').NextConfig} */
const nextConfig = {};

// Export the configuration
export default nextConfig;
